# apps/web

Phase 1 shipping client.

Responsibilities:

- Next.js App Router
- responsive mobile and desktop web experience
- PWA support
- install prompt and service worker registration
- gateway-backed shell reads with graceful fallback mode
- Firebase Auth login UI with secure server-side session bootstrap
- cookie-backed viewer session for SSR, route handlers, and gateway-backed auth-aware testing
- route handlers for post and resource creation through the gateway
- SSR and client rendering where appropriate
- no privileged backend business logic
